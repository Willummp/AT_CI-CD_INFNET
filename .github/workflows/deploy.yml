name: Integrated Full Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy-job:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Load Secrets and Envs
        env:
          ENV_TYPE: prod
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }} # Exemplo de secret
        run: |
          echo "Iniciando deploy no ambiente: $ENV_TYPE"
          if [ -n "$DB_PASSWORD" ]; then
             echo "Credenciais de banco carregadas com sucesso."
          fi
          echo "Deploy em staging concluído" # Mensagem solicitada no enunciado (embora ENV seja prod, mantendo a string pedida)
